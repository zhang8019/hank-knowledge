{
  "name": "hank-knowledge",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Knowledge Base plugin for Hana (Hanko Agent): managed material library with BM25 / vector / rerank retrieval, MinerU document parsing, and simple wiki summaries.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "node node_modules/typescript/bin/tsc --noEmit",
    "smoke": "node tests/smoke.mjs",
    "test:mineru": "node tests/mineru-v3.mjs",
    "test:wiki": "node tests/v3-features.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}